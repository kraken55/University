#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include <stdbool.h>
#include <math.h>

int main(int argc, char *argv[])
{
    if (argc != 2)
    {
        return -1;
    }

    int n = atoi(argv[1]);
    if (n <= 1)
    {
        return -1;
    }

    for (int i = pow(10, n); i < 
    )
}